import React from "react";

/**
 * Reserved for a future “home pick” strip on Today. Past-plan slides from the API live in
 * {@link PlanTemplateCarousel} inside {@link WorkoutEditor} (“Plans from history”) — do not duplicate
 * dummy Swiper slides here; they were mistaken for BE-driven templates.
 */
export function HomePickCarousel() {
  return null;
}
